{
  "name": "lab-manager",
  "private": true,
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "dev:api": "npm run dev --workspace @lab/api",
    "dev:web": "npm run dev --workspace @lab/web",
    "build": "npm run build --workspace @lab/api && npm run build --workspace @lab/web"
  }
}
